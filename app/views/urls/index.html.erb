<h1>Listing urls</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Url</th>
    <th>Comment</th>
    <th>Student</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @urls.each do |url| %>
  <tr>
    <td><%= url.name %></td>
    <td><%= url.url %></td>
    <td><%= url.comment %></td>
    <td><%= url.student_id %></td>
    <td><%= link_to 'Show', url %></td>
    <td><%= link_to 'Edit', edit_url_path(url) %></td>
    <td><%= link_to 'Destroy', url, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Url', new_url_path %>
